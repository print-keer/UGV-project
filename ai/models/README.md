# Models

Place trained weights for runtime inference in this directory.

Recommended default:

- `ai/models/best.pt`

The runtime CLI and ROS2 vision scaffold look for that path unless a different
model path is passed explicitly.
